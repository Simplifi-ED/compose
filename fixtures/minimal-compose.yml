# Smoke-test fixture: image MUST publish a linux/arm64 manifest (Apple Silicon).
# Do not substitute Intel-only (linux/amd64) images here.
services:
  web:
    image: docker.io/library/busybox:1.36.1
    command: httpd -f -p 80
    ports:
      - "18080:80"
