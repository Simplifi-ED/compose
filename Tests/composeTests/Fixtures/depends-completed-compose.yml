services:
  migrate:
    image: docker.io/library/alpine:3.20
    command: ["sh", "-c", "exit 0"]
  app:
    image: docker.io/library/alpine:3.20
    command: ["sleep", "300"]
    depends_on:
      migrate:
        condition: service_completed_successfully
