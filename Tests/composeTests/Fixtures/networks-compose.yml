name: demo
networks:
  backend: {}

services:
  client:
    image: docker.io/library/alpine:3.20
    command: sleep 300
    networks: [backend]
  server:
    image: docker.io/library/alpine:3.20
    command: sleep 300
    networks:
      backend: {}
