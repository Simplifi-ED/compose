secrets:
  first:
    file: ./secrets/db_password.txt
  second:
    file: ./secrets/app_config.txt
services:
  app:
    image: docker.io/library/alpine:3.20
    secrets:
      - source: first
        target: /run/secrets/shared
      - source: second
        target: /run/secrets/shared
