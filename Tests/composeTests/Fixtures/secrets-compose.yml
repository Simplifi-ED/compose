secrets:
  db_password:
    file: ./secrets/db_password.txt
services:
  app:
    image: docker.io/library/alpine:3.20
    command: ["cat", "/run/secrets/db_password"]
    secrets:
      - db_password
